services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=train_management'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=secret'
      - 'MYSQL_USER=adminDb'
    ports:
      - '3306:3306'
  redis:
    image: 'redis:latest'
    ports:
      - '6379:6379'
